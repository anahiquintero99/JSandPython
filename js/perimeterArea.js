function perimeterRectangle(base = 24, height = 13) {
  perimeter = (base + height) * 2;
  console.log(perimeter);
}

function surfaceRectangle(base = 24, height = 13) {
  surface = base * height;
  console.log(surface);
}

perimeterRectangle();
surfaceRectangle();
