function areaSquare(side = 4) {
  const area = side * side;
  console.log(area);
}

function perimeterSquare(side = 4) {
  const perimeter = side * 4;
  console.log(perimeter);
}

areaSquare();
perimeterSquare();
